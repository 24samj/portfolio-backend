{
  "name": "portfolio-backend",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "prepare": "husky",
    "build:seed": "bun run scripts/build-seed.ts",
    "db:generate": "drizzle-kit generate",
    "db:local": "wrangler d1 migrations apply portfolio-db --local",
    "db:remote": "wrangler d1 migrations apply portfolio-db --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.9.7",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@cloudflare/vitest-pool-workers": "^0.16.20",
    "@types/node": "^24.3.1",
    "drizzle-kit": "^0.31.10",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "typescript": "^5.9.2",
    "ultracite": "7.8.3",
    "vitest": "^4.1.2",
    "wrangler": "^4.42.0"
  },
  "lint-staged": {
    "*.{ts,js,mjs,cjs,json,jsonc,md}": [
      "ultracite fix"
    ]
  }
}
